specVersion: v0.1.0
package:
  name: 'substreams_postgresql_sink_nftLoader'
  version: v0.1.0

protobuf:
  files:
    - erc721.proto
  importPaths:
    - ./proto

# imports:
#   database_change: https://github.com/streamingfast/substreams-database-change/releases/download/v0.1.0/substreams-database-change-v0.1.0.spkg

binaries:
  default:
    type: wasm/rust-v1
    file: target/wasm32-unknown-unknown/release/substreams_postgresql_sink_nftLoader.wasm

modules:
  # - name: store_block_meta_start
  #   kind: store
  #   updatePolicy: set_if_not_exists
  #   valueType: proto:eth.block_meta.v1.BlockMeta
  #   inputs:
  #     - source: sf.ethereum.type.v2.Block
  - name: store_transfers
    kind: store
    initialBlock: 12287507
    updatePolicy: set_if_not_exists
    valueType: int64
    inputs:
      - source: sf.ethereum.type.v2.Block
      # - map: map_transfers

  # - name: db_out
  #   kind: map
  #   inputs:
  #     - store: store_block_meta_start
  #       mode: deltas
  #   output:
  #     type: proto:sf.substreams.database.v1.DatabaseChanges
  - name: db_out
    kind: map
    initialBlock: 12287507
    inputs:
      - store: store_transfers
        mode: deltas
    output:
      type: proto:sf.substreams.database.v1.DatabaseChanges # proto:eth.erc721.v1.Transfers

